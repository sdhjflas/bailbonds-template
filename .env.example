# URL where preview JSON files are stored.
# Local dev: leave blank (falls back to /api/v1/previews/:slug via LeadGrid on localhost:8000)
# Vercel production: set to your Cloudflare R2 public URL, e.g. https://pub-xxxx.r2.dev/previews
VITE_PREVIEW_DATA_URL=
